<!-- Add user profile pic? -->
<h1><%=@user.name%></h1>
<% if @user.followers.include?(User.find(session[:user_id])) %>
<%= link_to "Unfollow", destroy_relationship_path(@user.id), method: "delete"%>
<% else %>
<%= link_to "Follow", new_relationship_path(@user.id)%>
<% end %>
